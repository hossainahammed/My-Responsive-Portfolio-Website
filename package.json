{
  "name": "my-responsive-portfolio-website",
  "version": "1.0.0",
  "description": "Personal responsive portfolio website for Hossain Ahammed",
  "scripts": {
    "dev": "vite",
    "build": "vite build && powershell -Command \"if (Test-Path images) { Copy-Item -Recurse -Force images dist/images }\"",
    "preview": "vite preview",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "vite": "^5.2.11"
  },
  "dependencies": {
    "firebase": "^12.17.1"
  }
}
